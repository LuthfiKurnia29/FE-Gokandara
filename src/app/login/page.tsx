import LoginPage from '@/blocks/login/LoginPage';

/**
 * The main page component that renders the LoginPage component.
 *
 * @returns {JSX.Element} The rendered LoginPage component.
 */
const Page = () => {
    return (
        <main>
            <LoginPage />
        </main>
    );
};

export default Page;
